# 예외는 이렇게 적습니다

일반적인 소비 말고, 헷갈리기 쉬운 열두 가지를 모았습니다. 같은 내용이 앱의 [입력] 탭 맨 아래에도 접혀 있습니다.

먼저 뼈대. **하나의 칸에 여러 뜻을 담지 않습니다.**

| 축 | 칸 | 답하는 질문 |
|---|---|---|
| 업종 | 대분류, 소분류 | 어떤 곳에서 썼나 |
| 상황 | 상황 | 언제, 어떤 자리였나 |
| 대상 | 대접 | 누구에게 쓴 돈인가 |
| 맥락 | 테마 | 어떤 일에 딸린 지출인가 |
| 부담 | 누구 몫 | 내가 얼마를 부담했나 |

---

## 사람이 얽힌 것

### 1. 현금으로 드린 용돈
소분류 `용돈` + `대접` 에 받는 사람

### 2. 물건으로 드린 선물
소분류 `선물` + `대접` 에 받는 사람

업종(가전, 의류)이 아니라 `선물` 로 넣습니다. 여행지에서 산 것은 `여행 선물` 입니다. 소분류 이름이 겹치면 조회 수식이 엉키기 때문에 이름을 갈라 두었습니다. 예전에 `선물` 226건 1,768만원이 통째로 여행으로 잡힌 적이 있습니다.

### 3. 함께 먹은 밥과 커피
업종은 그대로 + `대접` 에 함께한 사람

밥과 커피는 선물이 아니라 대접이라 `한식`, `카페` 를 그대로 둡니다. 옛 가계부의 엄빠, 보은이 여기입니다. 연간요약의 [누구에게 쓴 돈인지] 표에서 용돈, 선물과 셋이 합쳐집니다.

### 4. 신세를 갚는 자리
`상황` 을 보은 + `대접` 에 그 사람

업종은 그대로 둡니다. 왜 그 자리였는지는 상황이 알려 줍니다.

---

## 소개팅과 연애

### 5. 소개팅
`테마` 소개팅 + `동행` 에 상대 이름 + `대접` 은 **비움**

이름은 [소개팅 이력] 시트의 동행 이름과 한 글자도 다르지 않아야 집계됩니다. 연애가 시작되면 그때부터는 데이트로 바꿉니다.

### 6. 연애 중 데이트
`테마` 데이트 + `대접` 연인 + `동행` 에 상대 이름

소개팅과 연애 기간이 겹치는 사람은 **연애 쪽으로만** 넣습니다. 양쪽에 넣으면 같은 돈을 두 번 세게 됩니다. 옛 시트에서 실제로 그렇게 새고 있었습니다.

---

## 나눠 낸 것

### 7. 둘이 반씩 낸 것
`금액` 은 전액 그대로 + `누구 몫` 을 연인과 반반 또는 동생과 반반

**미리 나눠서 적지 마세요.** 전액을 두고 비율로 나눠야 나중에 비율이 바뀌어도 되돌릴 수 있습니다. `내 몫` 은 저절로 계산됩니다.

### 8. 가족이 함께 다녀온 여행
`금액` 전액 + `누구 몫` 가족 넷이서 + `테마` 여행

가족계금으로 다녀온 여행입니다. 결제는 한 사람이 해도 부담은 넷이 나눕니다.

### 9. 남이 낸 것
`누구 몫` 을 남이 낸 것으로 (부담률 0)

기록은 남기되 내 지출에서는 빠집니다.

---

## 그 밖

### 10. 여행 중에 쓴 돈
업종이 아니라 `여행` 대분류 아래의 소분류 + `상황` 여행 중

여행지 카페는 `카페` 가 아니라 여행 아래의 음료 소분류입니다. 여행은 그 자체가 하나의 사건이라 따로 묶는 편이 실제로 쓸모 있었습니다.

### 11. 저축과 계좌이체
소분류를 저축, 투자, 계좌이체, 대출 상환 중에서

소비가 아니라 지출 합계에서 빠집니다. 생활비통장 납입과 여행계 납입이 여기입니다. 그 돈으로 산 것은 따로 적히므로, 여기까지 지출로 세면 두 번 셈이 됩니다.

### 12. 한 번으로 끝나는 큰 지출
`일시성` 을 Y 로

추이를 볼 때 걸러 내기 위한 표시입니다. 명절 용돈처럼 해마다 되풀이되는 것은 Y 가 아닙니다.

---

## 주선자와 소개 루트는 어디에 적나

**가계부 [입력] 시트가 아닙니다.** 메모에도 적지 않습니다.

`[소개팅 이력]` 시트에 사람당 한 줄, 거기 `소개 루트` 와 `주선자` 칸이 이미 있습니다. 실제로 168명이 그렇게 적혀 있습니다.

| 이름 | 소개 루트 | 주선자 |
|---|---|---|
| 김나윤 | 지인 소개 | 수철 |
| 수연 | 앱 | 틴더 |
| 장희수 | SNS | 블로그 |

가계부 줄에는 그 사람과 **언제 얼마를 썼는지**만 남고, 그 사람이 **누구를 통해 만난 사람인지**는 이력 시트에 남습니다. 같은 사람과 열 번 만나도 주선자는 한 번만 적으면 됩니다. 메모에 적으면 열 줄에 같은 말이 열 번 들어가고, 나중에 주선자별로 세어 보려 할 때 둘이 어긋납니다.

**앱이 대신 이어 줍니다.**

- `동행` 칸에 이름을 치면 이력 시트에 있는 이름들이 자동으로 따라옵니다 (지금 193명)
- 아는 이름이면 파란 줄로 알려 줍니다. `소개팅 / 김나윤 / 지인 소개 수철 / 첫 만남 2018-07-01 / 3번 만남`
- 처음 보는 이름이면 붉은 줄과 함께 [이력에 더하기] 단추가 뜹니다. 소개 루트(지인 소개 / 앱 / SNS / 직접 / 기타)와 주선자를 넣으면 앱이 이력 시트에 한 줄을 더합니다. 주선자도 이미 쓰던 59개가 자동 완성됩니다 (골드스푼, 틴더 같은 것들)

---

## 앱이 대신 지켜 주는 것

이제 입력 화면이 적는 동안 규칙을 봅니다. 외우지 않아도 됩니다.

| 이렇게 고르면 | 이 칸이 필수가 됩니다 |
|---|---|
| 테마 소개팅 | 동행 (그리고 대접은 비워야 합니다) |
| 테마 데이트 | 동행 + 대접 연인 (대접이 비어 있으면 연인으로 자동으로 채웁니다) |
| 소분류 용돈 | 대접 |
| 소분류 선물, 여행 선물 | 대접 |
| 상황 보은 | 대접 |
| 누구 몫 연인과 반반 | 동행 |
| 상황 여행 중 | 소분류가 여행 대분류 아래여야 합니다 |

모자란 칸은 **붉게 표시되고**, 폼 아래에 무엇을 채워야 하는지 한 줄로 뜹니다. 그 상태로는 저장 단추가 막힙니다. 규칙에 걸리지 않을 때는 지금 적고 있는 것이 어떤 예외인지 파란 줄로 알려 줍니다.

[고치기] 창에서도 똑같이 돕니다.
