/* 개인 설정. 이 파일만 채우면 앱이 돕니다.
   배포할 때 이 파일을 덮어쓰지 마세요. */
const CONFIG = {
  // 가계부 구글시트 ID (주소의 /d/ 와 /edit 사이 문자열)
  SHEET_ID: "",

  // Apps Script 웹앱 주소. [배포 > 배포 관리]의 복사 아이콘으로 가져오세요. /exec 로 끝납니다
  APPS_SCRIPT_URL: "",

  // 시트 이름
  SHEET_NAME: "입력",
  META_SHEET: "분류표",

  // 화면 파일 버전. Code.gs 를 고칠 때만 SERVER_EXPECTED 를 올립니다
  APP_VERSION: "v1.0",
  SERVER_EXPECTED: "1",

  OWNER: "주현",
};
