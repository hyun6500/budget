# 설치 안내

처음부터 다시 세우는 순서입니다. 서버를 `Code.gs` 한 파일로 새로 썼습니다. 예전 `Portfolio.gs` 와 `Ping.gs` 는 여기에 다 들어갔으니 따로 두지 않습니다.

---

## 어느 시트에 넣나

**가계부 시트입니다.** `[@] 주현 가계부 통합(2018~2026)` 쪽입니다.

```
https://docs.google.com/spreadsheets/d/1AWHmkn0TH_zayTLbRBmMNvNDP4KaJoZ9888JCEsBbVI/edit
```

왜 자산 시트가 아니냐면, **글을 쓰는 곳이 가계부**이기 때문입니다. 앱에서 기록을 더하고 고치는 대상이 `[입력]` 시트입니다. 스크립트를 그 시트에 붙여 두면 `getActiveSpreadsheet()` 로 바로 열리고 권한도 자동으로 따라옵니다.

자산 시트는 **읽기만** 합니다. 다른 파일이라 `openById` 로 따로 열고, 그래서 `PORTFOLIO_ID` 한 줄이 필요합니다.

```
자산 시트 ID: 1d84iPBCiuhfq-JGfd8LdYwVJXpDoIFh7a4JFQ9_2mGo
```

자산 시트에는 아무것도 설치하지 않습니다. 다만 **두 시트가 같은 계정 소유**여야 합니다. 아니라면 자산 시트를 가계부 계정에 편집자로 공유해 주세요.

---

## 1) 서버 세우기

1. 가계부 시트를 엽니다
2. **확장 프로그램 > Apps Script**
3. 왼쪽 파일 목록에 `Code.gs` 가 있습니다. **그 안의 내용을 전부 지우고** `apps-script/Code.gs` 를 통째로 붙여 넣습니다
4. 비밀번호와 키를 **스크립트 속성**에 넣습니다

   편집기 왼쪽 [프로젝트 설정] > [스크립트 속성] > [속성 추가]

   | 속성 | 값 |
   |---|---|
   | `PASSWORD` | 앱에서 기록을 더하거나 고칠 때 넣을 비밀번호 |
   | `GEMINI_KEY` | 화면 판독용 키. 안 쓰면 넣지 않아도 됩니다 |
   | `PORTFOLIO_ID` | `1d84iPBCiuhfq-JGfd8LdYwVJXpDoIFh7a4JFQ9_2mGo` |

   칸을 만들기 귀찮으면 편집기에서 `setupProps()` 를 한 번 실행하세요. 빈 칸 세 개가 생깁니다.

   **왜 속성인가.** 지금까지 `Code.gs` 를 두 번 통째로 갈아 끼웠습니다. 비밀번호와 키를 코드에 적어 두면 갈아 끼울 때마다 날아가고 다시 적어야 합니다. 속성에 두면 코드를 몇 번을 바꿔도 값이 그대로 남습니다.

   코드 맨 위 상수(`var PASSWORD` 등)는 그대로 두어도 됩니다. **속성이 비어 있을 때만** 그쪽을 봅니다. 둘 다 되니 편한 쪽으로 하세요.

5. 저장하고, 함수 목록에서 **`checkAll`** 을 골라 실행합니다. 처음 한 번은 권한 승인을 물어봅니다
6. 실행 기록을 봅니다. 이렇게 나오면 성공입니다

```
붙어 있는 시트: [@] 주현 가계부 통합(2018~2026)
[입력] 있음. 11538줄
[분류표] 있음
머리글 21개 확인
분류표: 소분류 106, 누구 몫 6, 상황 12, 대접 8, 테마 10, 결제수단 14
PASSWORD 설정됨 (속성)
GEMINI_KEY 설정됨 (속성)
자산 시트: [@] 종합 자산 포트폴리오(재테크) (속성)
자산 416190156 / 평가 387917295.5 / 기준일 2026-06-24
트리 31, 로그 8, 예적금 4, 매매 73, 연봉 14, 사이드잡 35, VC 4
VERSION 2
```

`checkAll` 은 아무것도 바꾸지 않습니다. 무엇이 빠졌는지만 알려 줍니다.

---

## 2) 배포

**배포 > 새 배포 > 톱니바퀴 > 웹 앱**

| 칸 | 값 |
|---|---|
| 설명 | 아무거나 |
| 실행 대상 | **나** |
| 액세스 권한 | **모든 사용자** |

액세스 권한이 '나만' 이면 구글이 로그인 페이지로 돌려보내고, 그 페이지에는 CORS 헤더가 없어서 앱에 **Failed to fetch** 가 뜹니다. 지난번 증상이 이것일 가능성이 큽니다.

배포하면 `/exec` 로 끝나는 주소가 나옵니다. **그 주소를 브라우저 주소창에 그대로 붙여 넣어 보세요.** 이렇게 보이면 됩니다.

```json
{"ok":true,"ping":"alive","version":"2",
 "boundSheet":"[@] 주현 가계부 통합(2018~2026)",
 "hasInput":true,"hasMeta":true,"passwordSet":true,
 "portfolioSheet":"[@] 종합 자산 포트폴리오(재테크)"}
```

로그인 화면이 뜨면 액세스 권한이 잘못된 것입니다.

---

## 3) 앱 올리기

1. `js/config.js` 를 열어 `APPS_SCRIPT_URL` 에 방금 그 `/exec` 주소를 넣습니다. **채울 곳은 이것 하나뿐입니다**
2. 폴더를 통째로 GitHub 저장소(`budget`)에 올립니다
3. `hyun6500.github.io/budget/` 로 엽니다

시트 ID, 비밀번호, Gemini 키는 `js/config.js` 에 두지 않습니다. 그 파일은 GitHub Pages 에 그대로 올라가 누구나 볼 수 있기 때문입니다. 전부 Apps Script 쪽에 있습니다.

---

## 코드를 고친 뒤에는

**배포 > 배포 관리 > 연필 > 버전 [새 버전] > 배포**

이걸 빼먹으면 주소는 그대로인데 옛 코드가 돕니다. 자주 겪는 함정입니다.

---

## 무엇이 되는지

| 요청 | 하는 일 | 잠금 |
|---|---|---|
| `rows` | 입력 시트와 분류표를 읽어 보냅니다 | 없음 |
| `login` | 비밀번호를 확인하고 15분짜리 표를 줍니다 | - |
| `add` | 날짜 차례에 맞는 자리에 줄을 끼워 넣습니다 | 필요 |
| `update` | no 로 줄을 찾아 고칩니다 | 필요 |
| `del` | no 로 줄을 찾아 지웁니다 | 필요 |
| `parse` | 화면과 글을 읽어 후보를 만듭니다 | 없음 |
| `assets` | 자산 시트를 읽습니다 | 필요 |

`add` 는 **no 를 다시 매기지 않습니다.** 언제나 `max + 1` 입니다. 원본보관 시트와의 짝이 끊어지지 않게 하기 위해서입니다. 대신 날짜 차례에 맞는 자리에 끼워 넣으므로 시트가 흐트러지지 않습니다.

대분류, 구분, 부담률, 내 몫, 연, 연월은 **수식이 아니라 값으로** 채웁니다. 손으로 고쳤을 때를 위해 시트 메뉴에 [파생 칸 다시 계산] 을 두었습니다.

---

## 시트 메뉴

시트를 새로 열면 상단에 **가계부 도구** 메뉴가 생깁니다.

- **날짜순으로 정렬** 입력 시트를 날짜, no 순으로 정렬합니다
- **파생 칸 다시 계산** 대분류, 구분, 부담률, 내 몫, 연, 연월을 다시 채웁니다
- **설치 상태 확인** `checkAll` 과 같습니다
- **스크립트 속성 칸 만들기** `PASSWORD`, `GEMINI_KEY`, `PORTFOLIO_ID` 빈 칸을 만들어 둡니다

---

## 확인해 본 것

실제 가계부(11,538줄)와 실제 자산 시트를 물려 서버를 통째로 돌려 봤습니다.

- `rows` 미리 깔아 둔 빈 줄을 걸러 9,038줄만 보냅니다. 분류표에서 소분류 106, 누구 몫 6, 상황 12, 대접 8, 테마 10, 결제수단 14를 읽습니다
- `login` 틀린 비밀번호는 거절, 맞으면 표 발급
- 잠긴 채로 `add` 를 부르면 거절
- `add` 2026-08-05 로 넣으면 08-05 와 08-06 사이에 정확히 들어갑니다. no 11539, 대분류 카페/간식, 구분 지출, 부담률 0.5, 내 몫 6,000 이 자동으로 채워졌습니다
- `update` 소분류를 한식으로 바꾸니 대분류가 식비로 따라오고, 누구 몫을 되돌리니 부담률 1, 내 몫 15,000 으로 다시 계산됩니다
- `del` 지운 뒤 행수가 원래대로 돌아옵니다
- `assets` 총액 416,190,156, 기준일 2026-06-24, 매매 73건
- 모르는 요청은 오류 메시지로 돌려줍니다
